name: Email Action Test

on:
  push:
    branches:
      - main  # Runs the action when changes are pushed to main

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: lisa.miranda@gramener.com
        run: echo "Hello, world!"
